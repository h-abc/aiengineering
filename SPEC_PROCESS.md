# SPEC_PROCESS.md — 聊天记录分析器

## 一、Brainstorming 过程

### 1.1 关键节点

与 Claude 进行 brainstorming，经历了四轮关键迭代：

**第一轮：完整方案（39 tasks）**
- 初始方案：Python + FastAPI + DeepSeek + 微信/Telegram 双平台 + 话题/情绪/人物三维分析 + Web 仪表盘 + 报告导出
- Claude 追问：用户为什么不用 ChatGPT 直接分析？你的工具不可替代的价值是什么？
- 我的回答：持久化、多维度批量、隐私控制、可视化、可复现
- Claude 追问：通用分析器还是先做微信+Telegram？
- 我的回答：先做微信，架构预留扩展点

**第二轮：精简版（31 tasks）**
- Claude 指出：大一作业 39 tasks 过于复杂
- 砍掉：Telegram 解析、Insights 多轮对话、报告导出、人物独立页面、筛选页面、暗色模式、Ollama 客户端
- 保留：微信导入、话题分析、情绪分析、Web 仪表盘、凭据管理、Docker 分发

**第三轮：极简版（18 tasks）**
- 继续精简：人物统计、清洗器 LLM 辅助、异步 Worker、聚合器、多页面 WebUI、溯源侧边栏
- 压缩为 7 个 Python 源文件 + 1 个 HTML 模板

**第四轮：纯静态版（5 tasks）**
- CI 持续失败：keyring 在 Linux CI 环境无后端、dbus 配置复杂
- 最终决策：放弃 Python 后端，改为纯静态 HTML + JS
- 情感词典替代 LLM 情绪分析，话题词典替代 LLM 话题聚类

### 1.2 关键迭代

| 轮次 | 架构 | Tasks | 问题 |
|---|---|---|---|
| 1 | Python 全栈 + LLM | 39 | 过于复杂 |
| 2 | Python 精简 | 31 | 仍复杂 |
| 3 | Python 极简 | 18 | CI keyring 依赖无法解决 |
| 4 | 纯静态 HTML | 5 | ✅ 最终方案 |

### 1.3 AI 建议的采纳与推翻

**采纳：**
- 全局话题聚类替代每日窗口 LLM 调用（节省 API 成本）
- 封闭式情绪标签（可聚合可比）
- 逐章 brainstorming 追问（先确认问题陈述，再进入用户故事，避免返工）
- 最终放弃 Python 后端（解决 CI 困境）

**推翻：**
- 初始方案过于复杂，经过三轮精简才收敛
- LLM 分析方案被最终放弃，改为规则化

### 1.4 反思：Brainstorming 技能评价

**做得好：**
- 分块追问：逐章确认，每块确认后才进入下一块
- 主动质疑复杂度：Claude 多次提醒"大一作业是否过于复杂"
- 方案对比：给出多个方案优缺点，让用户选择

**不足：**
- 初始 SPEC 容易过度设计，用户缺乏经验判断复杂度
- 应该更早考虑 CI 环境的兼容性问题
- 纯静态方案应该在更早阶段被提出

## 二、冷启动验证

### 2.1 验证配置
- 验证 agent 类型：未执行（项目架构变更为纯静态后，冷启动验证意义降低）
- 项目最终版本为单个 HTML 文件，无需 subagent 协作

## 三、反思

整个 brainstorming 过程最大的教训是：**初始方案总是过度设计**。我的第一个方案包含 39 个 task、6 个模块、双平台支持、LLM 三维分析，但实际交付时发现 CI 环境的一个依赖问题（keyring）就能让整个项目无法通过。经过四轮迭代，最终收敛到 5 个 task 的纯静态方案，功能反而更聚焦、更可用。

这说明在 AI 辅助开发中，**快速验证可行性比完善设计更重要**。如果我一开始就从最简单可行的方案起步，再逐步迭代，会节省大量时间。
