一、问题陈述

1.1 要解决的问题

在微信中，个人和团队每天都在产生大量对话。这些对话记录了沟通模式、情绪变化、话题演变和协作节奏，但几乎没有人能凭手动翻阅复盘"我们到底聊了什么"。

核心痛点：

个人知识工作者：想复盘过去一年与导师/朋友/客户的交流，但只能靠翻聊天记录，低效且无法总结趋势。目前只能手动翻阅或逐条搜索，没有自动化工具。

团队 Tech Lead / PM：想知道团队在哪个话题上耗时最多、大家的情绪是否因某个项目节点变差，但缺乏客观数据。现有方案（如企业微信后台）仅提供基础统计，不做语义分析。

社群运营者：想分析社群活动的周期性活跃高峰、用户关注话题变化，但没有工具能自动完成。

1.2 为什么值得做

第一，真实痛点。沟通是所有协作的核心，但复盘工具极度匮乏。市面上没有面向个人用户的聊天记录分析工具。

第二，零门槛。纯静态网页，无需安装、无需注册、无需 API Key，双击即用或 GitHub Pages 直接访问。

第三，隐私优先。所有数据在浏览器本地处理，不上传任何服务器，天然契合隐私需求。

第四，规则化分析。用情感词典和话题词典替代 LLM，既节省成本，又保证结果可复现、可解释。

1.3 目标用户

个人知识工作者：周五下午想回顾本周与导师的关键讨论，当前只能手动翻聊天记录，低效。

团队 Tech Lead：项目复盘时查看团队讨论热点和情绪变化，当前缺乏客观数据。

社群运营者：分析社群在某活动期间的活跃高峰，当前没有自动化工具。

学生：分析小组讨论记录，写团队协作报告，当前手动统计耗时。


二、用户故事

遵循 INVEST 原则：Independent（独立）、Negotiable（可协商）、Valuable（有价值）、Estimable（可估算）、Small（小粒度）、Testable（可测试）。

故事1：作为用户，我想上传微信 .txt 导出文件，这样我能一键导入聊天数据，无需手动整理。独立于其他故事；核心入口；单文件上传；可检查解析结果统计。

故事2：作为用户，我想看到解析后的统计摘要（消息总数、参与者数、时间跨度），这样我能快速确认导入是否成功。独立；即时反馈；数值展示；可检查统计数字准确性。

故事3：作为用户，我想查看话题分布图，这样我能一眼看出聊天中讨论最多的主题是什么。独立；核心价值；条形图；可检查话题词典匹配正确性。

故事4：作为用户，我想查看情绪趋势图，这样我能识别哪天的讨论情绪偏正面或负面。独立；核心价值；柱状图；可检查情感词典匹配正确性。

故事5：作为用户，我想查看活跃度分布（按小时/按天），这样我能知道聊天的活跃时段和节奏。独立；辅助洞察；两个柱状图；可检查统计数值正确性。

故事6：作为用户，我想在消息列表中按发送者筛选和关键词搜索，这样我能快速定位特定对话。独立；实用功能；下拉框加搜索框；可检查筛选和搜索正确性。

故事7：作为用户，我希望所有数据在浏览器本地处理，不上传服务器，这样我的隐私受到保护。贯穿全局；隐私安全；纯前端；断网后仍可用。


三、功能规约

模块1：微信解析器（Parser）

输入：用户通过文件选择器选择的微信 .txt 文件，编码 UTF-8。

行为：使用 FileReader 读取文件内容；按行分割；用正则匹配多种日期格式（2024-03-15 14:30:25、2024/3/15 14:30、2024年3月15日 14:30:25、3月15日 14:30、14:30）；提取发送者、时间戳、消息内容；过滤系统消息（撤回、加入群聊、拍了拍等）；为每条消息计算情绪标签。

输出：JavaScript 对象数组，每个对象包含字段：id（字符串）、sender_name（字符串）、timestamp（Unix毫秒）、date（YYYY-MM-DD格式）、hour（0到23）、dayOfWeek（0到6，周日为0）、content_type（text/image/file）、text_content（字符串）、sentiment（包含label、score、cls三个子字段）。

边界条件：空文件则提示"未能解析出消息"；非 .txt 文件不阻止但可能解析失败；单行超过10000字符正常处理；文件超过10MB浏览器可能卡顿，建议限制。

错误处理：编码错误则提示用户检查文件编码；正则不匹配则跳过该行；时间戳解析失败则跳过该行；全部行跳过则提示"未能解析出消息，请检查文件格式"。

模块2：话题分析器（Topic Analyzer）

输入：解析后的消息数组。

行为：话题词典匹配，遍历8个话题类别（版本发布、测试、需求讨论、技术讨论、时间协调、团队沟通、问题排查、学习分享），对每条消息检查是否包含该类别的关键词，统计命中数；词频统计，对所有消息进行简单分词（2到4字滑动窗口），去除停用词（100多个常用虚词），统计词频，取前20个高频词。

输出：包含 topics 数组（每个元素含 name 和 count）和 topWords 数组（每个元素含 word 和 count）。

边界条件：消息数少于10则话题可能不准确；无话题命中则展示"未检测到明显话题"；高频词可能包含无意义词，通过停用词过滤。

错误处理：所有消息均为图片或文件则话题为空，topWords 为空。

模块3：情绪分析器（Sentiment Analyzer）

输入：每条消息的文本内容。

行为：正面词匹配，遍历40多个正面情感词（好、棒、赞、谢谢、开心、哈哈、同意、支持、👍、😊等），统计命中数；负面词匹配，遍历40多个负面情感词（不好、差、失败、烦、焦虑、担心、延期、😡、😢等），统计命中数；判定规则为正面词多于负面词则为正面（score等于min(1,命中数除以3)），负面词多于正面词则为负面（score等于max(-1,负命中数除以3)），相等则为中性（score等于0）；每日聚合，按日期分组，计算平均分和主导情绪标签。

输出：每条消息的情绪对象包含 label（正面或负面或中性）、score（-1到1）、cls（CSS类名）；每日聚合数组，每个元素包含 date、score、label、count。

边界条件：纯图片消息标记为中性；emoji仅支持词典中的（👍😊😂😄🎉✅❤️😡😢😭😤💔👎❌）；中英文混合仅匹配中文词典。

错误处理：空消息为中性；情感词典未命中为中性。

模块4：活跃度分析器（Activity Analyzer）

输入：解析后的消息数组。

行为：按小时统计，遍历消息，统计每个小时（0到23）的消息数；按天统计，遍历消息，统计每周每天（周日到周六）的消息数。

输出：包含 hourly（24个整数的数组）、daily（7个整数的数组）、dayNames（周日到周六的字符串数组）。

边界条件：消息数少于50则图表可能不具代表性；跨多天则按天统计有意义。

错误处理：无错误情况。

模块5：消息列表（Message List）

输入：解析后的消息数组加上用户筛选条件。

行为：按发送者筛选，下拉框列出所有参与者，选择后只显示该发送者的消息；关键词搜索，输入框实时过滤，大小写不敏感；每条消息显示时间、发送者、内容、情绪标签；最多显示200条，按时间倒序。

输出：渲染后的HTML消息列表。

边界条件：筛选结果为空则显示"没有匹配的消息"；消息超过200条则截断并提示"显示200条消息"。

错误处理：搜索特殊字符正常处理，通过textContent转义。


四、非功能性需求

4.1 性能

文件解析速度目标为小于2秒（1000条消息），测量方法为浏览器Performance API。话题分析速度目标为小于500毫秒（1000条消息）。情绪分析速度目标为小于200毫秒（1000条消息）。页面首次加载目标为小于3秒（含CDN资源），测量方法为Lighthouse。文件大小限制建议小于10MB，受浏览器内存限制。消息数量无硬限制，超过5000条可能卡顿。

4.2 安全（含威胁模型）

由于本项目为纯静态网页，无后端、无数据库、无API调用，传统安全威胁大幅降低。

威胁一：聊天记录泄露到第三方服务器。风险等级为无风险，因为所有处理在浏览器本地完成，不发送任何网络请求（除CDN加载静态资源）。

威胁二：XSS跨站脚本攻击。攻击者为恶意聊天记录。风险等级为低。对策是消息内容通过textContent渲染，不使用innerHTML直接插入。

威胁三：浏览器localStorage泄露。攻击者为本地恶意软件。风险等级为低。对策是本项目不使用localStorage存储数据。

威胁四：CDN劫持。攻击者为网络中间人。风险等级为极低。对策是Chart.js和Pico.css从CDN加载使用HTTPS，可替换为本地文件。

威胁五：文件读取漏洞。攻击者为恶意.txt文件。风险等级为低。对策是FileReader仅读取文本不执行，正则解析失败时跳过。

凭据威胁模型：本项目不使用任何API Key，无凭据存储需求，因此无凭据威胁。

4.3 可用性

首次使用：打开网页即可使用，无需注册、登录、配置。文件格式兼容：支持5种微信导出日期格式，自动适配。错误提示：解析失败时显示明确错误原因。离线可用：加载后断网仍可使用（CDN资源已缓存）。标签页切换：4个标签页（话题、情绪、活跃度、消息），点击切换即时响应。响应式：支持桌面和移动端（Pico.css响应式布局）。

4.4 可观测性

解析统计：上传后显示消息数、参与者数、时间跨度。话题覆盖：显示话题命中数和高频词数量。情绪分布：显示正面、中性、负面占比。错误日志：浏览器Console输出解析失败的行（开发模式）。CI验证：GitHub Actions自动检查index.html完整性（文件存在、关键函数存在、CDN依赖存在）。


五、系统架构

5.1 组件图（文本描述）

整个系统运行在浏览器中。文件上传组件通过input元素接收用户选择的文件，将文件内容传递给微信解析器（parseWechat函数）。微信解析器将文本解析为消息数组（parsedMessages），然后分发给三个分析器：话题分析器（analyzeTopics函数）负责话题词典匹配和词频统计，情绪分析器（analyzeSentiment函数）负责情感词典匹配和每日聚合，活跃度分析器（analyzeActivity函数）负责按小时和按天统计。三个分析器的结果通过Chart.js渲染为图表（话题条形图、情绪柱状图、活跃度柱状图）。同时，消息数组还在消息列表中展示，支持按发送者筛选和关键词搜索，每条消息附带情绪标签。外部依赖为CDN加载的Chart.js 4.4和Pico.css 2。

5.2 数据流

第一步：用户选择文件，触发input的change事件，FileReader.readAsText()读取文件内容为UTF-8字符串。

第二步：微信解析器按换行符分割为行数组，逐行正则匹配（5种格式尝试），过滤系统消息（正则黑名单），情感词典匹配（40多正面词加40多负面词），输出parsedMessages数组（每条消息含sentiment字段）。

第三步：统计展示，统计总消息数、参与者数（去重）、时间跨度，渲染统计卡片。

第四步：用户切换标签页时触发分析。话题分析执行话题词典匹配和词频统计，渲染条形图和高频词标签。情绪分析执行每日聚合和正面负面中性占比计算，渲染柱状图和占比卡片。活跃度执行按小时数组和按天数组统计，渲染两个柱状图。

第五步：消息列表按时间倒序渲染（最多200条），发送者筛选通过下拉框onChange重新渲染，关键词搜索通过输入框onInput重新渲染。

5.3 外部依赖

Chart.js 4.4.0：来源为CDN（jsdelivr.net），用途为话题条形图、情绪柱状图、活跃度柱状图的渲染，通过script标签加载。

Pico.css 2：来源为CDN（jsdelivr.net），用途为页面样式（卡片、按钮、表格、响应式布局），通过link标签加载。

无LLM供应商依赖。本项目使用规则化分析（情感词典加话题词典），不调用任何外部API。

无后端依赖。纯静态网页，不需要Python、Node.js、数据库、服务器。


六、数据模型

6.1 消息对象（Message）

消息对象的字段如下：id为字符串类型，是唯一标识，如"msg_42"。sender_name为字符串类型，是发送者名称，如"张三"。timestamp为数值类型，是Unix毫秒时间戳，如1710484225000。date为字符串类型，是日期字符串，如"2024-03-15"。hour为数值类型，表示小时0到23，如14。dayOfWeek为数值类型，表示星期0到6（周日为0），如5表示周五。content_type为字符串类型，表示消息类型，取值为text、image或file。text_content为字符串类型，是消息文本内容，图片或文件显示为"[图片]"或"[文件]"。sentiment为对象类型，包含三个子字段：label（字符串，正面或负面或中性）、score（数值，-1.0到1.0）、cls（字符串，CSS类名）。

6.2 话题对象（Topic）

话题对象的字段如下：name为字符串类型，是话题名称，如"版本发布"。count为数值类型，是命中该话题的消息数。

6.3 情绪数据点（SentimentPoint）

情绪数据点的字段如下：date为字符串类型，是日期，如"2024-03-15"。score为数值类型，是当日平均情绪值，-1.0到1.0。label为字符串类型，是当日主导情绪，取值为正面、负面或中性。count为数值类型，是当日消息数。

6.4 存储约束

持久化：无持久化存储。所有数据在浏览器内存中，刷新页面后丢失。容量：受浏览器内存限制，建议小于10MB文件。隐私：数据不离开浏览器，无网络请求发送聊天内容。


七、凭据与分发设计

7.1 凭据存储

本项目不使用任何API Key，无凭据存储需求。

在需要API Key的早期版本（v3.0 Python版）中，曾设计Keyring存储方案（使用操作系统钥匙串）、环境变量方案（ANALYZER_LLM_API_KEY）和浏览器localStorage方案。v4.0已全部移除，改用规则化分析。

v4.0的凭据威胁模型：无凭据则无威胁。这是本项目安全设计的核心优势。

7.2 分发设计

主要分发方式为GitHub Pages。获取方式为浏览器访问 https://h-abc.github.io/aiengineering/。目标平台为所有平台（桌面和移动端）。

备用分发方式为本地文件。获取方式为下载index.html后双击打开。目标平台为所有平台（需浏览器）。

备用分发方式为任意静态托管。获取方式为上传index.html到Netlify、Vercel或Cloudflare Pages。目标平台为所有平台。

分发特点：零依赖，不需要安装任何运行时（Python、Node.js、Docker）。单文件，整个应用只有一个index.html文件。即时可用，GitHub Pages推送后1到2分钟生效。离线可用，首次加载后浏览器缓存CDN资源，断网仍可使用。


八、技术选型与理由

语言选择JavaScript（ES5兼容）。理由：浏览器原生支持，无需编译，无需打包工具。使用ES5语法确保兼容旧浏览器。

图表库选择Chart.js 4.4。理由：轻量（约60KB gzip压缩后）、API简洁、支持柱状图、折线图、条形图、社区活跃。从CDN加载，无需npm。

CSS框架选择Pico.css 2。理由：极轻量（约6KB gzip压缩后）、语义化HTML、内置响应式、暗色模式支持、无JS依赖。从CDN加载。

部署平台选择GitHub Pages。理由：免费、零配置、自动HTTPS、与GitHub仓库集成、推送即部署。

分析方式选择规则化（情感词典加话题词典）。理由：无需LLM API Key、零成本、可复现、可解释、隐私安全。

关于Open Design的偏离说明：课程要求推荐使用Open Design进行界面开发。本项目偏离此推荐的理由为，Open Design基于React组件体系，需要Webpack或Vite构建工具链，而本项目目标是"零依赖、单文件、双击可用"。替代方案为使用Pico.css提供基础样式，Chart.js处理图表，HTML原生控件处理交互。Open Design适合需要复杂UI组件的SPA应用，本项目为单页分析工具，UI复杂度低。


九、验收标准

模块1（微信解析器）的验收标准共5条。标准1.1：上传标准格式.txt文件，解析出正确消息数。测试方法为创建含10条消息的测试文件，上传后统计显示10。标准1.2：系统消息被过滤（撤回、加入群聊等）。测试方法为测试文件含1条系统消息，解析后不计入统计。标准1.3：支持斜杠日期格式"2024/03/15"。测试方法为使用该格式的测试文件，解析成功。标准1.4：空文件提示"未能解析出消息"。测试方法为上传空文件，显示错误提示。标准1.5：图片消息标记为image类型。测试方法为测试文件含"[图片]"，content_type为"image"。

模块2（话题分析器）的验收标准共3条。标准2.1：包含"发版"关键词的消息被归类到"版本发布"话题。测试方法为测试消息含"发版"，话题列表显示"版本发布"。标准2.2：高频关键词正确展示。测试方法为重复出现5次以上的词出现在Top 20。标准2.3：无话题命中时显示"未检测到明显话题"。测试方法为使用不包含任何话题关键词的测试文件。

模块3（情绪分析器）的验收标准共4条。标准3.1：含"开心"的消息被标记为正面。测试方法为测试消息含"开心"，sentiment.label等于"正面"。标准3.2：含"焦虑"的消息被标记为负面。测试方法为测试消息含"焦虑"，sentiment.label等于"负面"。标准3.3：无情感词的消息被标记为中性。测试方法为测试消息"今天天气不错"（无词典词），sentiment.label等于"中性"。标准3.4：正面、中性、负面占比之和为100%。测试方法为任意测试文件，三个占比相加等于100%。

模块4（活跃度分析器）的验收标准共2条。标准4.1：按小时统计正确。测试方法为测试文件含3条14:00的消息，hourly[14]等于3。标准4.2：按天统计正确。测试方法为测试文件含2条周五的消息，daily[5]等于2。

模块5（消息列表）的验收标准共3条。标准5.1：按发送者筛选正确。测试方法为选择"张三"，只显示张三的消息。标准5.2：关键词搜索正确。测试方法为输入"发版"，只显示含"发版"的消息。标准5.3：情绪标签正确显示。测试方法为正面消息显示绿色标签，负面显示红色。

跨模块通用验收标准共4条。标准C.1：GitHub Pages可访问。测试方法为浏览器打开https://h-abc.github.io/aiengineering/。标准C.2：CI unit-test job通过。测试方法为GitHub Actions显示绿色勾。标准C.3：离线可用。测试方法为加载后断网，切换标签页仍正常。标准C.4：移动端可用。测试方法为手机浏览器打开，布局正常。


十、风险与未决问题

10.1 风险

风险一：微信导出格式变更。影响为解析失败。概率为低。应对措施为支持5种格式，持续更新正则；解析失败时提示用户。

风险二：情感词典覆盖不足。影响为情绪分析准确率低。概率为中。应对措施为情感词典可扩展（目前40多正面词加40多负面词）；用户可手动查看消息列表验证。

风险三：话题词典覆盖不足。影响为话题识别不全面。概率为中。应对措施为话题词典可扩展（目前8类话题）；高频词词云可作为补充。

风险四：大文件浏览器卡顿。影响为用户体验差。概率为中。应对措施为建议限制小于10MB；消息列表截断200条。

风险五：Chart.js CDN不可用。影响为图表无法渲染。概率为极低。应对措施为jsdelivr全球CDN稳定性高；可替换为其他CDN或本地文件。

风险六：跨浏览器兼容性。影响为部分浏览器功能异常。概率为低。应对措施为使用ES5语法，兼容Chrome、Firefox、Safari、Edge。

10.2 未决问题

问题一：是否需要支持多文件上传？当前暂不支持，可扩展为一次上传多个.txt文件后合并分析。

问题二：是否需要导出分析报告？当前暂不支持，可添加导出为图片（截图）或PDF（浏览器打印）的功能。

问题三：情感词典是否需要扩展？当前90多词，可让用户自定义添加词典词条。

问题四：是否需要支持英文聊天记录？当前仅支持中文情感词，可添加英文情感词典。

问题五：是否需要暗色模式？当前暂不支持，Pico.css支持暗色模式，可添加切换按钮。

问题六：是否需要消息去重？当前暂不支持，微信导出本身不含重复消息，暂不需要。

问题七：图片或文件消息是否需要特殊处理？当前标记为image或file类型，未来可支持图片预览（需用户提供图片文件）。
